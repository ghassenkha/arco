<form method="post" action="{{ path('nomenclature_delete', {'id': nomenclature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nomenclature.id) }}">
    <button class="btn">Delete</button>
</form>

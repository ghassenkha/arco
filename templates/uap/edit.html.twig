{% extends 'base.html.twig' %}

{% block title %}Edit Uap{% endblock %}

{% block body %}
    <h1>Edit Uap</h1>

    {{ include('uap/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('uap_index') }}">back to list</a>

    {{ include('uap/_delete_form.html.twig') }}
{% endblock %}

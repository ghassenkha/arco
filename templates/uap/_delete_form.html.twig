<form method="post" action="{{ path('uap_delete', {'id': uap.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ uap.id) }}">
    <button class="btn">Delete</button>
</form>

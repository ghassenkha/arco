{% extends 'base.html.twig' %}

{% block title %}Uap{% endblock %}

{% block body %}
    <h1>Uap</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ uap.id }}</td>
            </tr>
            <tr>
                <th>Poste</th>
                <td>{{ uap.poste }}</td>
            </tr>
            <tr>
                <th>Section</th>
                <td>{{ uap.section }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('uap_index') }}">back to list</a>

    <a href="{{ path('uap_edit', {'id': uap.id}) }}">edit</a>

    {{ include('uap/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Edit Eclat{% endblock %}

{% block body %}
    <h1>Edit Eclat</h1>

    {{ include('eclat/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('eclat_index') }}">back to list</a>

    {{ include('eclat/_delete_form.html.twig') }}
{% endblock %}

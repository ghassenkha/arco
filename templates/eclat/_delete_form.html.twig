<form method="post" action="{{ path('eclat_delete', {'id': eclat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eclat.id) }}">
    <button class="btn">Delete</button>
</form>
